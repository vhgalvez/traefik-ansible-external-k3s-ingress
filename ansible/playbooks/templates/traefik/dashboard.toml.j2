
[http.middlewares.auth.basicAuth]
  usersFile = "/etc/traefik/secrets/users.htpasswd"

[http.services]
  [http.services.dummy.loadBalancer]
    [[http.services.dummy.loadBalancer.servers]]
      url = "http://127.0.0.1"


[http.routers.dashboard]
  rule = "Host(`traefik.interna.tuempresa.com`) && PathPrefix(`/dashboard`)"
  entryPoints = ["traefik"]
  service = "api@internal"
  middlewares = ["auth", "internal-whitelist"]

[http.middlewares.internal-whitelist.ipWhiteList]
  sourceRange = ["192.168.1.0/24"]