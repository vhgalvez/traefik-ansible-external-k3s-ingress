# 🌐 Traefik Environment Variables (usadas en docker-compose.yml)

# Dirección del API Server del clúster Kubernetes
KUBERNETES_SERVER=https://{{ vip }}:6443

# Namespace donde se ejecuta Traefik
TRAEFIK_NAMESPACE=traefik

# Directorio base de configuración de Traefik
TRAEFIK_CONFIG_DIR={{ traefik_dir }}

# Ruta a los certificados TLS autofirmados
TLS_CERT_FILE={{ traefik_dir }}/certs/traefik.local.crt
TLS_KEY_FILE={{ traefik_dir }}/certs/traefik.local.key

# Token para acceso a la API de Kubernetes
# TRAEFIK_TOKEN_FILE={{ traefik_token_path }}