vrrp_instance VI_1 {
    state MASTER
    interface {{ interface }}
    virtual_router_id 51
    priority {{ priority_lb1 }}  # 🔹 Aquí se usa priority_lb1
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass secretpass
    }
    virtual_ipaddress {
        {{ vip_address }}
    }
}