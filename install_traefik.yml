---
- name: Instalación y configuración de Traefik en LoadBalancers
  hosts: loadbalancer
  become: true
  vars:
    traefik_config_dir: /etc/traefik
    docker_compose_file: /etc/traefik/docker-compose.yml
    acme_file: /etc/traefik/acme.json
    traefik_docker_image: "traefik:v3.2"
    letsencrypt_email: "tu-email@dominio.com"
    traefik_domain: "cefaslocalserver.com"
    firewall_ports:
      - 80/tcp
      - 443/tcp
      - 8080/tcp
      - 6443/tcp

  tasks:
    - name: Instalar paquetes necesarios
      ansible.builtin.yum:
        name:
          - epel-release
          - yum-utils
          - net-tools
          - vim
          - curl
          - wget
        state: present

    - name: Agregar el repositorio de Docker
      ansible.builtin.shell: |
        dnf config-manager --add-repo=https://download.docker.com/linux/centos/docker-ce.repo

    - name: Instalar Docker y Docker Compose
      ansible.builtin.yum:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-compose
        state: present

    - name: Iniciar y habilitar Docker
      ansible.builtin.systemd:
        name: docker
        state: started
        enabled: true

    - name: Crear directorios para Traefik
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: '0755'
      loop:
        - "{{ traefik_config_dir }}"
        - "{{ traefik_config_dir }}/ssl"

    - name: Copiar certificados SSL personalizados
      ansible.builtin.copy:
        src: "files/ssl/{{ item }}"
        dest: "/etc/traefik/ssl/{{ item }}"
        mode: '0600'
      loop:
        - wildcard.cefaslocalserver.com.crt
        - wildcard.cefaslocalserver.com.key

    - name: Crear archivo acme.json
      ansible.builtin.file:
        path: "{{ acme_file }}"
        state: touch
        mode: '0600'

    - name: Copiar configuración de Docker Compose
      ansible.builtin.template:
        src: templates/docker-compose.yml.j2
        dest: "{{ docker_compose_file }}"

    - name: Copiar configuración de Traefik
      ansible.builtin.template:
        src: templates/traefik.toml.j2
        dest: "{{ traefik_config_dir }}/traefik.toml"

    - name: Copiar configuración dinámica de Traefik
      ansible.builtin.template:
        src: templates/dynamic_conf.toml.j2
        dest: "{{ traefik_config_dir }}/dynamic_conf.toml"

    - name: Iniciar Traefik con Docker Compose
      ansible.builtin.shell:
        cmd: docker compose up -d
        chdir: "{{ traefik_config_dir }}"

    - name: Configurar firewall
      ansible.builtin.firewalld:
        port: "{{ item }}"
        permanent: true
        state: enabled
      loop: "{{ firewall_ports }}"
      notify: Reload firewall

  handlers:
    - name: Reload firewall
      ansible.builtin.service:
        name: firewalld
        state: reloaded